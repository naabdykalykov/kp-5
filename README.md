# 📝 Notes App — Express.js + React

Полноценное веб-приложение для управления заметками с REST API на Express.js и React-фронтендом.

## 🎯 Функционал

- ✅ **CRUD операции** — создание, чтение, обновление, удаление заметок
- ✅ **Поиск** — поиск заметок по заголовку и содержимому
- ✅ **Фильтрация** — фильтрация по категориям
- ✅ **Категории** — личное, работа, покупки, идеи

## 🏗️ Структура проекта

```
kp-5/
├── server/                    # Backend (Express.js)
│   ├── index.js              # Главный файл сервера
│   ├── routes/
│   │   └── notes.js          # Маршруты для заметок
│   ├── controllers/
│   │   └── notesController.js # Логика обработки запросов
│   ├── middleware/
│   │   └── logger.js         # Собственный middleware
│   └── data/
│       └── notes.json        # Хранилище данных
├── src/                      # Frontend (React)
│   ├── App.jsx               # Главный компонент
│   ├── App.css               # Стили приложения
│   └── main.jsx              # Точка входа
├── public/                   # Статические файлы
├── package.json              # Зависимости проекта
└── vite.config.js            # Конфигурация Vite
```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Режим разработки

**Терминал 1** — запуск Express-сервера:
```bash
npm run server
```

**Терминал 2** — запуск Vite dev-сервера:
```bash
npm run dev
```

Откройте http://localhost:5173

### 3. Production-сборка

```bash
npm run start
```

Откройте http://localhost:3000

## 📡 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/notes` | Получить все заметки |
| `GET` | `/api/notes?category=work` | Фильтр по категории (query-параметр) |
| `GET` | `/api/notes/search?q=текст` | Поиск заметок |
| `GET` | `/api/notes/:id` | Получить заметку по ID (req.params) |
| `POST` | `/api/notes` | Создать заметку (req.body) |
| `PUT` | `/api/notes/:id` | Обновить заметку |
| `DELETE` | `/api/notes/:id` | Удалить заметку |

### Примеры запросов

**Создание заметки:**
```bash
curl -X POST http://localhost:3000/api/notes \
  -H "Content-Type: application/json" \
  -d '{"title": "Новая заметка", "content": "Текст заметки", "category": "work"}'
```

**Поиск:**
```bash
curl "http://localhost:3000/api/notes/search?q=проект"
```

**Фильтрация:**
```bash
curl "http://localhost:3000/api/notes?category=personal"
```

## ⚙️ Используемые технологии

### Backend
- **Express.js** — веб-фреймворк
- `express.json()` — парсинг JSON в теле запроса
- `express.urlencoded()` — парсинг URL-encoded данных
- `express.static()` — раздача статических файлов

### Frontend
- **React 19** — UI библиотека
- **Vite** — сборщик

### Особенности реализации

1. **Модульная структура** — routes и controllers разделены
2. **Собственный middleware** — логирование запросов с временем выполнения
3. **Query-параметры** — фильтрация (`?category=`) и поиск (`?q=`)
4. **Route-параметры** — доступ по ID (`/api/notes/:id`)
5. **Статические файлы** — раздача собранного React-приложения

## 📸 Скриншоты

### Главная страница
![Главная страница](screenshots/main.png)

### Создание заметки
![Создание заметки](screenshots/create.png)

### API ответ
![API ответ](screenshots/api.png)

## 👤 Автор

Курсовая работа по дисциплине "Веб-разработка"

---

*Express.js + React Notes Application*
